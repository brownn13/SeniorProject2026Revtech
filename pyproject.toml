[project]
name = "revtech"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.1",
    "pandas>=3.0.3",
    "plotly>=6.9.0",
    "pytest>=9.1.1",
    "requests>=2.34.2",
    "streamlit[auth]>=1.59.2",
]

[project.scripts]
revtech = "revtech:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
